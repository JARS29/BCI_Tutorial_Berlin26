# --- Core analysis stack -----------------------------------------------
# Needed for BCI_Tutorial_HandsOn.ipynb (data loading, cleaning, ICA, CSP,
# classification, evaluation, plotting).
mne>=1.6
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
PyWavelets>=1.4
pyriemann>=0.5
joblib>=1.3

# --- Experiment design & live decoding -----------------------------------
# Needed for BCI_Tutorial_ExperimentDesign.ipynb (real stimulus window),
# experiment_stimulus.py, send_eeg_data.py, and lsl_online_decoder.py.
# Not required to run BCI_Tutorial_HandsOn.ipynb or the experiment-design
# notebook in its mock mode (e.g. on Google Colab).
psychopy>=2024.1.1
pylsl>=1.16

# --- Running the notebooks -----------------------------------------------
jupyter>=1.0
ipykernel>=6.29
